"use client";

/**
 * BoilerRuntimeList -- per-room boiler toggle + runtime bars, grouped by floor.
 * Stub created in Task 1; fully implemented in Task 2.
 */
export default function BoilerRuntimeList() {
  return <div>Loading boilers...</div>;
}
