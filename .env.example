# ============================================================
# AegeanSea Hotel Manager — Environment Variables
# ============================================================
# This file is loaded automatically by PM2 via dotenv.
# Copy to .env and fill in all values before starting the app.
# Generate AUTH_SECRET with: openssl rand -base64 33

# --- Home Assistant Connection ---
HA_URL=http://10.10.90.10:8123
HA_TOKEN=your-long-lived-access-token

# --- Auth.js v5 ---
AUTH_SECRET=replace-with-generated-secret
AUTH_TRUST_HOST=true

# --- Property ---
PROPERTY_NAME=AegeanSea Hotel
NEXT_PUBLIC_PROPERTY_NAME=AegeanSea Hotel

# --- Database ---
# Absolute path to the SQLite database file.
DB_PATH=/opt/hotel-manager/data/hotel.db

# --- Server ---
NODE_ENV=production
PORT=3000

# --- E2E Testing (optional) ---
# TEST_EMAIL=admin@hotel.local
# TEST_PASSWORD=your-password
# BASE_URL=http://localhost:3000
