# Auth.js v5 — generate with: openssl rand -base64 33
AUTH_SECRET=replace-with-generated-secret

# Multi-origin session support (LAN + Tailscale + Cloudflare)
AUTH_TRUST_HOST=true

# Property display name (shown on login screen and PWA manifest)
PROPERTY_NAME=AegeanSea Hotel
NEXT_PUBLIC_PROPERTY_NAME=AegeanSea Hotel

# SQLite database path (absolute path on CT101)
DB_PATH=/opt/hotel-manager/data/hotel.db

NODE_ENV=production
PORT=3000
