# ============================================================
# AegeanSea Hotel Manager — Environment Variables
# ============================================================
# Copy this file to .env and fill in all values.
# Generate AUTH_SECRET with: openssl rand -base64 33

# --- Home Assistant Connection ---
HA_URL=http://10.10.90.10:8123
HA_TOKEN=your-long-lived-access-token

# --- Auth.js v5 ---
AUTH_SECRET=replace-with-generated-secret
AUTH_TRUST_HOST=true

# --- Property ---
PROPERTY_NAME=AegeanSea Hotel
NEXT_PUBLIC_PROPERTY_NAME=AegeanSea Hotel

# --- Database ---
# Absolute path inside the container. The Docker bind mount maps
# host ./data/ to container /app/data/. Do NOT change unless you
# also change the volume mount in docker-compose.yml.
DB_PATH=/app/data/hotel.db

# --- Server ---
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
